<% if @user_net_votes - 1 < - 1 %>
  alert('You cannot down vote more than once. You may still up vote, though.');
<% else %>
  $('#post_<%= @post.id %>').html('<%= pluralize(@post.net_votes,'vote') %>');
<% end %>
